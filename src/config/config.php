<?php

/*
 * Copyright (c) 2026 Besnovatyj. Licensed under the MIT License.
 */

declare(strict_types=1);

/**
 * Дефолты модуля тегов. Значения `params.*` переопределяются модулем настроек `yii2-cms-config`
 * (опции объявлены в config/options.php).
 */
return [
    'id'     => 'Tags',
    'params' => [
        'iconClass' => 'bi bi-tags',

        /** Размер страницы выдачи `/tag/<slug>`. */
        'perPage' => 20,

        /** Сколько тегов показывает облако и страница `/tags`. */
        'cloudLimit' => 30,

        /**
         * Сколько секунд живут счётчики видимых записей у тегов (облако, страница `/tags`).
         *
         * Видимость записи меняет контентный модуль, и модуль тегов об этом не узнаёт — поэтому
         * счётчики не инвалидируются событием, а протухают по времени. Назначение тегов сбрасывает
         * их сразу (тег кэша `tags`).
         */
        'countsTtl' => 600,

        'directories' => false,
    ],
];
